Based on [riscv-tests](https://github.com/riscv/riscv-tests).
Includes target environment `p` virtual memory disabled, only core 0
boots up and test virtual machine `rv32ui` RV32 user-level, integer
only.
